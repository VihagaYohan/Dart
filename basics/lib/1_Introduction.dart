void main() {
  print("Hello, world");

  // use assert method to test
  assert(2 + 3 == 5);
}

// variables
var name = 'Bob';

// variable with null safety
String? name1 = 'Bob';

// final and const if value never get change
final name2 = 'Bob'; // without type annotation
final String name3 = 'Bob';

const bar = 1000;

